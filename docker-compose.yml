version: '3.8'

services:
  bot:
    build: .
    container_name: telegram_casino_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=Europe/Moscow

volumes:
  bot_data:
    driver: local
